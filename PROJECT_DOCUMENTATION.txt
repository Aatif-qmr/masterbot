━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
          CIPHER + QNT SYSTEM: MASTER DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Distributed Intelligence-First Trading Architecture
Version: 1.0.0
Updated: Sunday, May 3, 2026
Nodes: M1 (Execution), M2 (Intelligence)
Network: Tailscale Encrypted Mesh
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. EXECUTIVE OVERVIEW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
The Cipher system is a distributed algorithmic trading cluster managed by 
QNT, an autonomous intelligence layer. It operates a $50,000 USDT paper 
trading portfolio across five distinct market regimes.

Identity: QNT
Role: System Architect, Risk Auditor, and Intelligence Engine.
Mission: Maintain 100% uptime, enforce non-negotiable risk rules, and 
continuously evolve trading strategies using heavy compute (M2).

2. NODE ARCHITECTURE & DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
The system is split across two physical machines connected via Tailscale.

[M1] EXECUTION NODE (MacBook Air M1)
- IP: 100.90.68.42
- Role: Real-time bot execution, risk gating, and UI hosting.
- Path: /Users/aatifquamre/cipher/
- Core Components: 
    * 5 Freqtrade instances supervised by 'supervisord'.
    * Ports: 8080 (MR), 8081 (TF), 8082 (Scalp), 8083 (Swing), 8084 (Daily).
    * Local SQLite databases for trade history.
    * 'caffeinate' process to prevent system sleep.

[M2] INTELLIGENCE NODE (MacBook Air M2)
- IP: 100.74.110.36
- Role: Computationally intensive tasks (ML Training, Hyperopt, NLP).
- Path: /Users/azmatsaif/cipher/
- Core Components:
    * FreqAI acceleration (XGBoost/LightGBM).
    * ChromaDB (The Vault) for semantic search/memory.
    * Puppeteer Browser Engine for data extraction.
    * Full Python 3.12 venv matching M1.

NETWORKING:
- Passwordless SSH: M1 can execute commands on M2 via `ssh azmatsaif@100.74.110.36`.
- Memory Sync: Every 5 minutes, M1 mirrors its JSON memory to M2 via SCP.
- Browser Bridge: M1 triggers M2's browser engine for macro data fetching.

3. THE STRATEGY CLUSTER (ACTIVE)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
The $50,000 portfolio is divided into 10k per instance.

1. DailyTrendV1 (1d): BTC/ETH trend following. High timeframe stability.
2. TrendFollowV1 (4h): Multi-EMA momentum. Requires score > 0.3 sentiment.
3. MeanReversionV1 (1h): RSI + Bollinger Band. Trades in Neutral/Bullish regimes.
4. SwingV1 (15m): Volatility-adjusted swing trading.
5. ScalpV1 (5m): High-frequency order flow patterns.

CORE LOGIC:
- Informative Pairs: All strategies fetch 1h/1d data for macro trend alignment.
- Dynamic Paths: Strategies auto-detect node (M1 vs M2) for path injection.
- Fail-safes: Handle DataProvider gaps and 'pandas_ta' attribute errors.

4. RISK MANAGEMENT (THE SHIELD)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Risk rules are non-negotiable and enforced at the code level.

CLUSTER-AWARE PROTECTIONS:
- Daily Drawdown: 3.0% limit ($1,500). All entries blocked until next day.
- Weekly Drawdown: 7.0% limit ($3,500). All entries blocked. Manual review.
- Position Sizing: Max 10% ($5,000) of total cluster balance per trade.
- Order Rate: Max 10 trades per hour (Circuit Breaker).
- Stop Loss: Hard stop losses (2% to 8%) mandatory on all strategies.
- Aggregation: 'risk_manager.py' queries all 5 ports to get real $50k total.

COOLDOWN MECHANISM:
- Atomic Alerts: Uses file-locking (fcntl) to prevent 5 bots from spamming.
- Frequency: CRITICAL alerts sent maximum once per hour.

5. QNT SUPER CLI (THE 6 PILLARS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Unified command layer available on both machines.

I. THE BRIDGE (Control)
- qnt-bot [status|start|stop|restart]: Unified cluster control.
- qnt-logs [--follow]: Aggregated log stream from all 5 bots.

II. THE ORACLE (Intelligence)
- qnt-calendar: Scrapes economic events (CPI, FOMC) to set risk levels.
- qnt-sentiment: 4-source scoring (Reddit, Gecko, F&G, Funding).
- qnt-anomaly: Detects market structural shifts using ML.

III. THE SHIELD (Risk)
- qnt-balance: Live view of $50k total + Daily/Weekly P&L.
- qnt-risk-check: 8-point audit of current system safety.
- qnt-exposure: Real-time table of current capital at risk.

IV. THE LAB (Research)
- qnt-strategy-gen "idea": Generates Freqtrade Python files from text.
- qnt-backtest [strategy]: Triggers multi-year tests on M2 compute.
- qnt-optimize [strategy]: Bayesian param sweep on M2 cores.

V. THE VAULT (Memory)
- qnt-journal "note": Saves insights to ChromaDB vector storage.
- qnt-recall "query": Semantic search through years of trade logs.
- qnt-library: AI-driven trade analysis and pattern discovery.

VI. THE COCKPIT (Visualization)
- qnt-dashboard: Full-screen terminal dashboard (Textual/Rich).

6. AUTOMATION SCHEDULE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
M1 (EXECUTION) CRONTAB:
- Hourly: Global balance tracking & Risk audit.
- Hourly: System health diagnostic.
- Every 5 Min: Memory synchronization (M1 -> M2).
- Mon 7am: Weekly performance report.
- Sun 2am: Full system backup (tar.gz).

M2 (INTELLIGENCE) CRONTAB:
- Every 30 Min: Sentiment scraping & scoring.
- Mon/Thu 1am: FreqAI ML retraining (60-day window).
- Sun 11pm: Cluster-wide Hyperopt sweep.

7. OPERATING RULES FOR AI AGENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. YOLO MODE: Full permission to execute shell commands and file edits.
2. VERIFICATION: Every change must be validated with a test command.
3. CREDENTIALS: Never print or commit .env, API keys, or secrets.
4. PATHS: Always use absolute paths or dynamic home-dir resolution.
5. VENV: Always use '/Users/aatifquamre/cipher/venv/bin/python'.
6. SPAM: Respect alert cooldowns and file-locking protocols.

8. RECOVERY & TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- Bot Loop/Spam: Run `pkill -f freqtrade` followed by `rm /tmp/risk_alert_cooldown`.
- M2 Unreachable: Check Tailscale status and SSH key permission (600).
- Module Errors: Use the `qnt-python` wrapper to ensure venv context.
- Database Lock: Delete `.shm` and `.wal` files in `user_data/` if SQLite hangs.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
                     CIPHER: ARCHITECTED FOR AUTONOMY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
