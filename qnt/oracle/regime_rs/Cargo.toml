[package]
name = "regime-rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "regime_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.24", features = ["extension-module"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
