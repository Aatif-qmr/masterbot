#!/bin/bash
BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
# qnt-exposure
# Unified risk and exposure aggregator

$BASE_DIR/qnt/bin/qnt-python $BASE_DIR/qnt/src/exposure_aggregator.py "$@"
