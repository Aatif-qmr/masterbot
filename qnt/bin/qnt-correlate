#!/bin/bash
BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
# qnt-correlate
# Analyzes trade correlations between strategies

$BASE_DIR/qnt/bin/qnt-python $BASE_DIR/qnt/src/correlate.py "$@"
