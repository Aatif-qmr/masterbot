#!/bin/bash
BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
# qnt-library: Show vault statistics

set -a
source $BASE_DIR/.env
set +a

ssh azmatsaif@100.74.110.36 "
$BASE_DIR/venv/bin/python -c \"
import sys
sys.path.insert(0, '$BASE_DIR/qnt/vault')
from vault import get_collection_stats
stats = get_collection_stats()
print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━')
print('📚 QNT STRATEGY LIBRARY (THE VAULT)')
print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━')
print(f'Total Lessons/Entries: {stats.get(\\\"entry_count\\\", 0)}')
print(f'Database Engine:       ChromaDB (Persistent)')
print(f'Location:              M2 Intelligence Node')
print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━')
\"
"
