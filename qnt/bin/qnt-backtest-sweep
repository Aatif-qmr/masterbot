#!/bin/bash
BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
# qnt-backtest-sweep
# Runs a strategy against multiple market regimes

$BASE_DIR/qnt/bin/qnt-python $BASE_DIR/qnt/src/backtest_sweep.py "$@"
