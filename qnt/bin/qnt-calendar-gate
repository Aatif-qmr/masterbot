#!/bin/bash
BASE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
# qnt-calendar-gate [status|pause|resume|auto]
# Unified macro risk gate for Cipher

$BASE_DIR/qnt/bin/qnt-python $BASE_DIR/qnt/src/calendar_gate.py "$@"
